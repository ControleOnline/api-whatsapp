checks:
  javascript: true

build:
  nodes:
    analysis:
      project_setup:
        override: true
      tests:
        override:
          - js-scrutinizer-run

filter:
  excluded_paths:
    - "data/*"
    - "node_modules/*"
    - "public/*"
